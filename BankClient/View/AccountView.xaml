<UserControl x:Class="BankClient.View.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="157" d:DesignHeight="28">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" FontWeight="Bold" Content="{Binding AccountNumber}" HorizontalAlignment="Left"></Label>
        <Label Grid.Column="1" Grid.Row="0" FontWeight="Bold" Content="{Binding Description}" HorizontalAlignment="Left"></Label>
    </Grid>
</UserControl>
